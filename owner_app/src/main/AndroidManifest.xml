<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/owner_app"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.AppCompat.Light.DarkActionBar"
        tools:targetApi="31">

        <!-- Owner Login Activity -->
        <activity
            android:name=".OwnerLoginActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Owner Dashboard Activity -->
        <activity
            android:name=".OwnerDashboardActivity"
            android:exported="false" />

        <!-- Owner Products Activity -->
        <activity
            android:name=".OwnerProductsActivity"
            android:exported="false" />

        <!-- Owner Bills Activity -->
        <activity
            android:name=".OwnerBillsActivity"
            android:exported="false" />

    </application>

</manifest>